# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'EncryptDBUI' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for EncryptDBUI
  pod 'FMDB/SQLCipher'
  target 'EncryptDBUITests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'EncryptDBUIUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
